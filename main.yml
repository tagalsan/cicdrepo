job1:

        runs-on: ubuntu-latest

        steps:

            - name: Checkout Code

              uses: actions/checkout@v2

            - name: Run job1

              run: echo "job1...."

    job2:

        runs-on: ubuntu-latest

        needs: job1

        steps:

            - name: Checout Code

              uses: actions/checkout@v2

            - name: Run job2

              run: echo "job2...."

    job3:

        runs-on: ubuntu-latest

        needs: job1

        steps:

            - name: Checout Code

              uses: actions/checkout@v2

            - name: Run job3

              run: echo "job3......"
    job4:

        runs-on: ubuntu-latest

        needs: job2

        steps:

            - name: Checout Code

              uses: actions/checkout@v2

            - name: Run job4

              run: echo "job4...."
     job5:

        runs-on: ubuntu-latest

        needs: job3

        steps:

            - name: Checout Code

              uses: actions/checkout@v2

            - name: Run job5

              run: echo "job5...."
     job6:

        runs-on: ubuntu-latest

        needs: [job4, job5]

        steps:

            - name: Checout Code

              uses: actions/checkout@v2

            - name: Run job6

              run: echo "job6...."
